"""
ATK Agent Configuration Template
复制此文件为 config.py 并填入你的配置
"""
import os

# ATK 安装路径
ATK_EXE_PATH = r"D:\atk-v4.0.0\atk\ATK-v4.0.0-windows-x64\ATK-4.0.0\ATK.exe"

# ATK Connect 连接参数
ATK_HOST = "127.0.0.1"
ATK_PORT = 6655

# ATK 启动等待时间（秒）
ATK_STARTUP_WAIT = 8

# 输出目录
OUTPUT_DIR = os.path.join(os.path.dirname(__file__), "output")

# ========== LLM 配置（必填） ==========
# 支持 DeepSeek、OpenAI、Qwen 等兼容接口
LLM_API_KEY = "sk-your-api-key-here"          # ← 替换为你的 API Key
LLM_MODEL = "deepseek-chat"                    # 模型名称
LLM_BASE_URL = "https://api.deepseek.com/v1"   # API 地址
LLM_API_URL = f"{LLM_BASE_URL}/chat/completions"
