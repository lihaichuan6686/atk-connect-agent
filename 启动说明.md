# ATK Agent 1.4.1 启动说明

## 第一次安装

打开 PowerShell：

```powershell
cd D:\atk-v4.0.0\atk-agent-cli
npm.cmd install
npm.cmd run setup:python
npm.cmd run build
```

所有 npm 下载已配置走 `127.0.0.1:7897`。请先确保代理正在运行。

项目使用自己的 Python：

```text
D:\atk-v4.0.0\atk-agent-cli\.venv\Scripts\python.exe
```

它不会再启动或依赖 `atk_agent_fin` 目录中的 Python 环境。不要改用 MSYS2 Python 3.14。

## 核对 `.env`

```dotenv
ATK_EXE_PATH=D:\atk-v4.0.0\atk\ATK-v4.0.0-windows-x64\ATK-4.0.0\ATK.exe
ATK_PYTHON_PATH=D:\atk-v4.0.0\atk\ATK-v4.0.0-windows-x64\ATK-4.0.0\IntegratingWithATK\connect\Python
PYTHON_EXE=D:\atk-v4.0.0\atk-agent-cli\.venv\Scripts\python.exe
ATK_HOST=127.0.0.1
ATK_PORT=6655

# 可选：必须是纯英文绝对路径；用于 ATK SaveAs/Load/导出暂存
ATK_AGENT_ASCII_TEMP=C:\Users\Public\AppData\Local\Temp\atk-agent
```

即使工程目录或 `ATK_AGENT_HOME` 含中文，传给 ATK 的文件仍会先写入上述纯 ASCII 临时目录，再复制回目标位置。

## 第一次启动 TUI

```powershell
node .\bin\atk-agent.mjs chat --allow-new-atk
```

界面会让你选择模型服务商，填写 API Base URL、API Key 和模型。API Key 使用 Windows DPAPI 加密，不写入项目 `.env`。

`--allow-new-atk` 只表示：当 Session 尚未绑定且系统里没有 ATK 时，允许创建一个新实例。若已有 ATK，程序仍会要求你明确选择；若已绑定 PID 连接失败，程序会直接报错，绝不会偷偷启动第二个 ATK。

以后日常启动：

```powershell
cd D:\atk-v4.0.0\atk-agent-cli
node .\bin\atk-agent.mjs chat
```

TUI 默认使用 `tui-main` Session。退出 TUI 后，常驻 daemon、sidecar 和 ATK 继续工作，下次可以接着控制同一工程。

## 如果电脑上已经开着 ATK

```powershell
node .\bin\atk-agent.mjs instance list
node .\bin\atk-agent.mjs instance attach --pid 33436 --session tui-main
node .\bin\atk-agent.mjs chat
```

把示例 PID 换成 `instance list` 显示的真实 PID。

## 查看当前状态

```powershell
node .\bin\atk-agent.mjs daemon status --session tui-main
node .\bin\atk-agent.mjs instance current --session tui-main
node .\bin\atk-agent.mjs operation status --session tui-main
node .\bin\atk-agent.mjs tool call get_atk_runtime_state --session tui-main --json-args '{}'
```

TUI 中可输入 `/status`、`/tools`、`/help`、`/clear`、`/config`、`/quit`。

## 长任务卡住或超时

```powershell
node .\bin\atk-agent.mjs operation cancel --session tui-main
node .\bin\atk-agent.mjs operation wait --session tui-main --timeout 60000
node .\bin\atk-agent.mjs operation wait --session tui-main --recover --timeout 60000
```

客户端不再等待并不代表 ATK 内部计算已经停止。取消后产品会先标记为 failed；重新连接和探测成功后才允许 `--recover` 回到 idle。

## 比赛时推荐用独立 Session

```powershell
node .\bin\atk-agent.mjs daemon start --session competition
node .\bin\atk-agent.mjs instance list
node .\bin\atk-agent.mjs instance attach --pid 33436 --session competition
node .\bin\atk-agent.mjs run "读取当前场景和资源状态" --session competition --timeout 600000
```

建模前先运行成本估算：

```powershell
node .\bin\atk-agent.mjs tool call estimate_scene_cost --session competition --json-args '{"object_count":597,"duration_seconds":31536000,"step_seconds":1800}'
```

如果预计星历点超过默认 200 万，资源保护器会拒绝继续。应缩短窗口、增大步长或只把少量候选升级为完整 Satellite。

## 安全停止

只停止 Agent 服务、保留 ATK：

```powershell
node .\bin\atk-agent.mjs daemon stop --session tui-main
```

结束 Session，并仅在归属和 PID 身份验证通过时关闭由该 Session 创建的 ATK：

```powershell
node .\bin\atk-agent.mjs session close tui-main
```

不要用任务管理器按进程名批量结束 ATK。

## 常见问题

- 提示 `ATTACH_FAILED`：运行 `instance list`，再用 `instance attach --pid ... --session ...` 明确选择。
- 提示 `DAEMON_PERMISSION_MISMATCH`：daemon 与本次 CLI 的 Windows 权限级别不同。请全部使用普通权限，或全部使用管理员权限；程序不会再启动第二个 daemon。
- 提示 `OPERATION_BUSY`：先看 `operation status`，等待或取消旧操作。
- 提示 `RESOURCE_LIMIT`：缩小对象数/时间跨度、增大步长，或谨慎调整 `.env` 阈值。
- ATK 返回 `NACK`：表示命令或当前对象状态不满足要求。普通 NACK 不会再把整个 Session 锁死。
- 安装无法联网：检查本机代理是否监听 `127.0.0.1:7897`。
- 修改模型配置：运行 `node .\bin\atk-agent.mjs config`。
